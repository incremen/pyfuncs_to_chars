# pyfuncs_to_chars

Represent any character using only Python builtin function calls. No numeric literals, no string literals, no operators. Each function takes exactly one argument.
Details as to how and why in the website: (link to the website that I'll add later)
Uses flask for server stuff, sqllite.
Used claude opus for basically the entire frontend, I really couldn't be bothered with html